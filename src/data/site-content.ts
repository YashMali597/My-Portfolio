// AUTO-GENERATED by `npm run knowledge:build` from /knowledge.
// DO NOT EDIT BY HAND — your changes will be overwritten.
//
// /knowledge is the single source of truth for both the static pages and the
// agent. This module exists only because the browser cannot read markdown off
// disk; it is a build artifact, like lib/knowledge/index.json.
//
// To change any content below, edit the corresponding file under /knowledge
// and re-run `npm run knowledge:build`.

export interface ContactInfo {
  email: string;
  phone: string;
  linkedin: string;
  github: string;
}

export interface Profile {
  name: string;
  navbarBrand: string;
  roles: string[];
  targetRoles: string[];
  tagline: string;
  photo: string;
  resumeUrl: string;
  about: string[];
  contact: ContactInfo;
  footer: string;
}

export interface ExperienceEntry {
  title: string;
  company: string;
  /** Empty when undocumented in /knowledge — render the absence, don't guess. */
  dates: string;
  bullets: string[];
  stack: string[];
}

export interface EducationEntry {
  degree: string;
  school: string;
  score: string;
  years: string;
  location: string;
}

export interface SkillCategory {
  title: string;
  items: string[];
}

export interface Achievement {
  id: string;
  title: string;
  description: string;
  date: string;
  linkedinUrl: string;
  tags?: string[];
}

export interface PipelineStage {
  id: string;
  label: string;
  description: string;
  /** lucide-react icon name, resolved to a component in PipelineDiagram. */
  icon?: string;
  status?: "active" | "idle" | "warning";
}

export interface ProjectSections {
  problem: string;
  architecture: string;
  decisions: string;
  challenges: string;
  impact: string;
}

export interface Project {
  id: number;
  slug: string;
  title: string;
  desc: string;
  tech: string;
  categories: string[];
  skills: string[];
  stack: string[];
  timeframe: string;
  links: { repo: string; demo: string; writeup: string };
  sections: ProjectSections;
  pipelineStages?: PipelineStage[];
  pipelineLegend?: string[];
  sourceFile: string;
  /** Raw markdown body from /knowledge, rendered by the project detail page. */
  bodyMarkdown: string;
}

export const profile: Profile = {
  "name": "Yash Mali",
  "navbarBrand": "Yash Mali | Data → Solutions",
  "roles": [
    "AI Engineer",
    "Data Scientist",
    "AI Product Manager"
  ],
  "targetRoles": [
    "AI Architect",
    "Forward Deployed Engineer"
  ],
  "tagline": "Transforming data and software into intelligent, AI-driven product solutions",
  "photo": "/yash.jpg",
  "resumeUrl": "/resume.pdf",
  "about": [
    "I’m an AI engineer passionate about building intelligent systems that transform enterprise data into actionable insights and decision-support tools.",
    "My work focuses on integrating machine learning models, LLM-powered assistants, and scalable data pipelines into enterprise platforms to automate knowledge access, improve reporting workflows, and enable data-driven decision-making. I’ve developed ETL pipelines across legacy systems, built AI-driven internal analytics assistants to streamline information retrieval, and implemented predictive models to support experimentation and business optimization.",
    "I’m particularly interested in applying machine learning, generative AI, and behavioral analytics to solve real-world problems in areas such as experimentation, forecasting, product performance, and operational efficiency. Through my academic and professional experience, I’ve worked on customer segmentation, churn prediction, and uplift-based targeting systems using large-scale behavioral datasets.",
    "I enjoy working at the intersection of data science, engineering, and product teams to translate complex business challenges into scalable, AI-enabled solutions that drive measurable impact."
  ],
  "contact": {
    "email": "yashmali597@gmail.com",
    "phone": "+12144754785",
    "linkedin": "https://www.linkedin.com/in/yash-v-mali/",
    "github": "https://github.com/YashMali597"
  },
  "footer": "© 2026 Yash Mali | Data-Driven Portfolio"
};

export const terminalFocusLines: string[] = [
  "orchestrating multi-agent AI systems",
  "engineering ETL pipelines across enterprise platforms",
  "tuning Azure AI Search indexers for retrieval",
  "shipping LLM-powered production workflows"
];

export const experience: ExperienceEntry[] = [
  {
    "title": "AI Agents and Automation Intern",
    "company": "C.H. Guenther",
    "dates": "Jun 2026 - Present",
    "bullets": [
      "Designing and building multi-step agentic workflows in production, architected multi-agent systems using LangChain and LangGraph with tool calling integrations into SAP and Workday enterprise systems; deployed via Microsoft Copilot Studio with Azure AI Search knowledge base for context retrieval and memory",
      "Building an enterprise-wide agentic analytics platform, orchestrating specialized agents across cost, finance, and operations domains; owning the system end-to-end from architecture design through production deployment",
      "Built a Fabric end-to-end pipeline from 6 APIs through Bronze/Silver/Gold layers into a semantic model and Fabric Data Agent for natural language querying, saving ~70% of weekly reporting effort",
      "Built an auto-updating data pipeline connected to a cost analysis application for current and future modeling, reducing effort by ~60%"
    ],
    "stack": [
      "Python",
      "LangGraph",
      "LangChain",
      "LangFuse",
      "Azure AI Foundry",
      "Microsoft Copilot",
      "Microsoft Fabric",
      "PySpark",
      "Docker",
      "PyDantic"
    ]
  },
  {
    "title": "Graduate Software Engineer Trainee",
    "company": "Emerson",
    "dates": "Jan 2024 - Jul 2025",
    "bullets": [
      "Engineered ETL data migration pipelines across enterprise platforms using C# and .NET, reducing manual errors by 60% and improving interoperability",
      "Developed protocol-aware MODBUS and HART data conversion workflows, maintaining 90%+ data integrity",
      "Built an LLM-powered internal analytics assistant using Python and prompt engineering, reducing enterprise knowledge retrieval time by 45%",
      "Automated end-to-end data ingestion, model execution, and deployment using REST APIs and Azure DevOps CI/CD pipelines",
      "Implemented 15+ data validation rules through system log analysis to improve data quality in production ML pipelines",
      "Collaborated with cross-functional stakeholders to define KPIs and execute 150+ feature-level test scenarios aligned with business requirements"
    ],
    "stack": [
      "C#",
      ".NET",
      "Python",
      "REST APIs",
      "Azure DevOps",
      "MODBUS",
      "HART",
      "LLMs",
      "prompt engineering"
    ]
  },
  {
    "title": "Deep Learning Intern",
    "company": "Wizphys AI",
    "dates": "Jun 2023 - Dec 2023",
    "bullets": [
      "Built and shipped real-time exercise detection features for a physiotherapy mobile app using PoseNet keypoint extraction and PyTorch-based classification models",
      "Engineered skeletal keypoint pipelines and optimized models via hyperparameter tuning and cross-validation, improving prediction accuracy by 15%",
      "Deployed ML-driven features to production users, increasing session completion and engagement by 18% through iterative performance improvements"
    ],
    "stack": [
      "Python",
      "PyTorch",
      "PoseNet",
      "computer vision",
      "mobile deployment"
    ]
  }
];

export const education: EducationEntry[] = [
  {
    "degree": "MS in Information Technology Management",
    "school": "University of Texas at Dallas",
    "score": "GPA - 3.75/4.0",
    "years": "2025 – 2027",
    "location": "Richardson, Texas, USA"
  },
  {
    "degree": "Bachelor of Technology",
    "school": "VIT Pune, India",
    "score": "CGPA - 8.77/10.0",
    "years": "2020 – 2024",
    "location": "Pune, India"
  }
];

export const skills: SkillCategory[] = [
  {
    "title": "Programming & Development",
    "items": [
      "Python, SQL, C#, JavaScript, TypeScript",
      "REST APIs, FastAPI",
      ".NET, MVC Architecture",
      "Git, Azure DevOps CI/CD",
      "Docker"
    ]
  },
  {
    "title": "AI & Machine Learning",
    "items": [
      "Machine Learning, NLP",
      "LLMs (GPT, Claude-style models)",
      "Prompt Engineering, Agentic Workflows",
      "GenAI POC Development",
      "AI API Integration, Workflow Automation"
    ]
  },
  {
    "title": "Frameworks & Tools",
    "items": [
      "PyTorch, TensorFlow",
      "scikit-learn, OpenCV",
      "LangChain"
    ]
  },
  {
    "title": "Data & Analytics",
    "items": [
      "EDA, Predictive Modeling",
      "A/B Testing, Behavioral Analysis",
      "Power BI, Tableau",
      "KPI Tracking, Product Analytics"
    ]
  },
  {
    "title": "Product Skills",
    "items": [
      "Product Strategy",
      "Requirements Gathering",
      "User Stories",
      "Feature Prioritization",
      "Experimentation"
    ]
  },
  {
    "title": "Applied AI & Enterprise Integration",
    "items": [
      "Azure AI Search indexers (building and tuning search indexes for retrieval)",
      "Copilot Studio integrations (building conversational agents on top of enterprise data)",
      "LLM failure benchmarking (systematic evaluation of where and why LLM outputs fail)"
    ]
  }
];

export const projectCategories: string[] = [
  "All",
  "AI",
  "Data",
  "Software"
];

export const projects: Project[] = [
  {
    "id": 1,
    "slug": "ai-causal-intelligence-system",
    "title": "AI Causal Intelligence System",
    "desc": "Built an uplift-based targeting system using causal inference on 100K+ user A/B simulation data to identify high-impact segments and optimize budget allocation, simulating $684K+ incremental revenue.",
    "tech": "Python, scikit-learn, Causal Inference, A/B Testing, Predictive Modeling, Data Analytics",
    "categories": [
      "AI",
      "Data"
    ],
    "skills": [
      "Causal Inference",
      "Uplift Modeling",
      "A/B Testing",
      "Predictive Modeling",
      "Data Analytics"
    ],
    "stack": [
      "Python",
      "scikit-learn"
    ],
    "timeframe": "",
    "links": {
      "repo": "",
      "demo": "",
      "writeup": ""
    },
    "sections": {
      "problem": "Most targeting models answer the wrong question. A propensity model tells you\nwho is likely to convert — but a large share of those people would have\nconverted anyway, so spending budget on them buys nothing. The question that\nactually determines ROI is *who converts **because** of the treatment*, which is\na causal question, not a predictive one.\n\nThis system was built to answer that: estimate per-segment uplift from A/B data\nand allocate budget against incremental impact rather than raw likelihood.",
      "architecture": "An uplift-modeling pipeline over 100K+ users of A/B simulation data:\n\n- **A/B simulation data layer** — treatment and control outcomes across 100K+\n  users, forming the basis for causal estimation.\n- **Uplift / causal inference models** — scikit-learn models estimating the\n  incremental effect of treatment per user rather than the raw conversion\n  probability.\n- **Segment identification** — users grouped by estimated uplift, separating\n  genuinely persuadable segments from those who convert regardless (or who react\n  negatively to treatment).\n- **Budget allocation layer** — spend directed toward high-uplift segments,\n  producing the simulated incremental revenue figure.",
      "decisions": "**Uplift over propensity.** The central decision of the project: model the\n*incremental* effect of treatment, not the probability of conversion. This is\nwhat makes the budget allocation defensible.\n\n**Simulated A/B data as the substrate.** Working from A/B simulation data means\nthe ground-truth treatment effect is knowable, so the uplift estimator can be\nevaluated against something rather than assumed correct — which is the hard part\nof causal work on observational data.\n\n**Allocate at the segment level.** Per-user uplift estimates are noisy;\naggregating into segments makes the allocation stable and gives the business a\nunit it can act on.",
      "challenges": "**Uplift is unobservable per user.** You never see both the treated and untreated\noutcome for the same person, so the target variable has to be constructed\nindirectly — which makes both training and evaluation substantially harder than\nstandard classification.\n\n**Noise dominates the signal.** Treatment effects are typically much smaller than\nbaseline conversion variation, so uplift estimates are low-signal and require\ncareful validation to avoid allocating budget against noise.\n\n**Evaluating an uplift model.** Standard classification metrics don't apply;\nassessment needs uplift-specific curves rather than accuracy or AUC.",
      "impact": "Simulated **$684K+ incremental revenue** by reallocating budget toward\nhigh-uplift segments identified from **100K+ user A/B simulation data**.\n\nNote that this figure is a **simulated** outcome from the A/B simulation dataset,\nnot a realized business result — it should always be described that way."
    },
    "sourceFile": "knowledge/projects/ai-causal-intelligence-system.md",
    "bodyMarkdown": "Built an uplift-based targeting system using causal inference on 100K+ user A/B\nsimulation data to identify high-impact segments and optimize budget allocation,\nsimulating $684K+ incremental revenue.\n\n## Problem\n\nMost targeting models answer the wrong question. A propensity model tells you\nwho is likely to convert — but a large share of those people would have\nconverted anyway, so spending budget on them buys nothing. The question that\nactually determines ROI is *who converts **because** of the treatment*, which is\na causal question, not a predictive one.\n\nThis system was built to answer that: estimate per-segment uplift from A/B data\nand allocate budget against incremental impact rather than raw likelihood.\n\n## Architecture\n\nAn uplift-modeling pipeline over 100K+ users of A/B simulation data:\n\n- **A/B simulation data layer** — treatment and control outcomes across 100K+\n  users, forming the basis for causal estimation.\n- **Uplift / causal inference models** — scikit-learn models estimating the\n  incremental effect of treatment per user rather than the raw conversion\n  probability.\n- **Segment identification** — users grouped by estimated uplift, separating\n  genuinely persuadable segments from those who convert regardless (or who react\n  negatively to treatment).\n- **Budget allocation layer** — spend directed toward high-uplift segments,\n  producing the simulated incremental revenue figure.\n\n## Key decisions\n\n**Uplift over propensity.** The central decision of the project: model the\n*incremental* effect of treatment, not the probability of conversion. This is\nwhat makes the budget allocation defensible.\n\n**Simulated A/B data as the substrate.** Working from A/B simulation data means\nthe ground-truth treatment effect is knowable, so the uplift estimator can be\nevaluated against something rather than assumed correct — which is the hard part\nof causal work on observational data.\n\n**Allocate at the segment level.** Per-user uplift estimates are noisy;\naggregating into segments makes the allocation stable and gives the business a\nunit it can act on.\n\n## Challenges\n\n**Uplift is unobservable per user.** You never see both the treated and untreated\noutcome for the same person, so the target variable has to be constructed\nindirectly — which makes both training and evaluation substantially harder than\nstandard classification.\n\n**Noise dominates the signal.** Treatment effects are typically much smaller than\nbaseline conversion variation, so uplift estimates are low-signal and require\ncareful validation to avoid allocating budget against noise.\n\n**Evaluating an uplift model.** Standard classification metrics don't apply;\nassessment needs uplift-specific curves rather than accuracy or AUC.\n\n## Impact\n\nSimulated **$684K+ incremental revenue** by reallocating budget toward\nhigh-uplift segments identified from **100K+ user A/B simulation data**.\n\nNote that this figure is a **simulated** outcome from the A/B simulation dataset,\nnot a realized business result — it should always be described that way."
  },
  {
    "id": 2,
    "slug": "commodity-intelligence-platform",
    "title": "Commodity Intelligence Platform",
    "desc": "An automated pipeline that extracts raw commodity/market data, processes it through a Microsoft Fabric medallion architecture (bronze → silver → gold), and surfaces it in Direct Lake mode Power BI dashboards for near-real-time decision-making.",
    "tech": "Microsoft Fabric, Power BI (Direct Lake), Medallion Architecture (Bronze/Silver/Gold)",
    "categories": [
      "Data",
      "AI"
    ],
    "skills": [
      "Data Engineering",
      "Medallion Architecture",
      "ETL",
      "Dashboarding",
      "Data Modeling"
    ],
    "stack": [
      "Microsoft Fabric",
      "Power BI (Direct Lake)",
      "Medallion Architecture (Bronze/Silver/Gold)"
    ],
    "timeframe": "",
    "links": {
      "repo": "",
      "demo": "",
      "writeup": ""
    },
    "sections": {
      "problem": "Commodity and market data arrives continuously from external feeds, but the\ndecisions it should inform are time-sensitive. A traditional import-and-refresh\nBI setup puts a scheduled refresh between the data and the decision — the\ndashboard is always some hours behind reality, and the gap is invisible to\nwhoever is reading it.\n\nThe platform's job is to get raw market feeds into a trustworthy, query-ready\nshape and in front of decision-makers with as little latency as the reporting\nlayer allows.",
      "architecture": "Five stages, implemented as a **Microsoft Fabric medallion architecture** with a\nDirect Lake reporting layer. These stage descriptions are migrated verbatim from\nthe interactive pipeline diagram on the previous site.\n\n**1. Raw Extraction** *(icon: Database)*\nRaw commodity and market data is pulled from source feeds into the pipeline,\nunchanged and untransformed — the entry point for everything downstream.\n\n**2. Bronze — Raw Ingest** *(icon: Layers)*\nExtracted data lands in the `bronze` layer exactly as received — a raw,\nappend-only landing zone that preserves the original records for lineage and\nreprocessing.\n\n**3. Silver — Cleansed** *(icon: Filter)*\nBronze records are cleaned, validated, and conformed into a consistent schema in\nthe `silver` layer, removing duplicates and standardizing formats.\n\n**4. Gold — Curated** *(icon: Gem)*\nSilver data is aggregated into business-ready tables in the `gold` layer — the\ncurated, query-optimized layer that reporting tools read from.\n\n**5. Direct Lake Dashboards** *(icon: LayoutDashboard, status: active)*\nPower BI queries the `gold` layer directly in `Direct Lake` mode — no import or\nscheduled refresh — so dashboards reflect near-real-time data for\ndecision-making.",
      "decisions": "**Direct Lake instead of Import mode.** This is the decision that makes the\n\"near-real-time\" claim true rather than aspirational. Import mode would put a\nscheduled refresh between the gold layer and the dashboard; Direct Lake has\nPower BI read the gold tables in place, so there is no refresh window to fall\nbehind in.\n\n**Append-only bronze.** Landing data exactly as received — untransformed — costs\nstorage but buys lineage and full reprocessing. If a transformation rule turns\nout to be wrong, silver and gold can be rebuilt from bronze without going back\nto the source feeds.\n\n**Cleaning in silver, aggregation in gold.** Keeping deduplication and schema\nconformance separate from business aggregation means each layer has one job, and\na bad number can be traced to the layer that produced it.\n\n**Gold optimized for query, not for completeness.** The gold layer is shaped for\nwhat the dashboards ask, which is what allows Direct Lake to stay fast.",
      "challenges": "**Direct Lake has constraints Import mode doesn't.** Reading gold tables in place\nmeans the gold layer's physical shape directly determines dashboard performance\n— modeling mistakes surface as slow visuals rather than slow refreshes.\n\n**Schema drift from upstream feeds.** External commodity/market sources change\nshape without notice, and an append-only bronze layer means the drift lands\nintact and has to be absorbed in the bronze → silver transformation.\n\n**Deduplication across an append-only landing zone.** Because bronze never\noverwrites, silver carries the full burden of resolving duplicate and superseded\nrecords into a single consistent view.",
      "impact": "Delivered near-real-time commodity and market visibility to decision-makers by\nremoving the scheduled-refresh lag between the curated data layer and the\ndashboards reading it."
    },
    "pipelineStages": [
      {
        "id": "extract",
        "label": "Raw Extraction",
        "description": "Raw commodity and market data is pulled from source feeds into the pipeline, unchanged and untransformed — the entry point for everything downstream.",
        "icon": "Database",
        "status": "idle"
      },
      {
        "id": "bronze",
        "label": "Bronze — Raw Ingest",
        "description": "Extracted data lands in the `bronze` layer exactly as received — a raw, append-only landing zone that preserves the original records for lineage and reprocessing.",
        "icon": "Layers",
        "status": "idle"
      },
      {
        "id": "silver",
        "label": "Silver — Cleansed",
        "description": "Bronze records are cleaned, validated, and conformed into a consistent schema in the `silver` layer, removing duplicates and standardizing formats.",
        "icon": "Filter",
        "status": "idle"
      },
      {
        "id": "gold",
        "label": "Gold — Curated",
        "description": "Silver data is aggregated into business-ready tables in the `gold` layer — the curated, query-optimized layer that reporting tools read from.",
        "icon": "Gem",
        "status": "idle"
      },
      {
        "id": "direct-lake",
        "label": "Direct Lake Dashboards",
        "description": "Power BI queries the `gold` layer directly in `Direct Lake` mode — no import or scheduled refresh — so dashboards reflect near-real-time data for decision-making.",
        "icon": "LayoutDashboard",
        "status": "active"
      }
    ],
    "pipelineLegend": [
      "Direct Lake mode",
      "near real-time"
    ],
    "sourceFile": "knowledge/projects/commodity-intelligence-platform.md",
    "bodyMarkdown": "An automated pipeline that extracts raw commodity/market data, processes it\nthrough a Microsoft Fabric medallion architecture (bronze → silver → gold), and\nsurfaces it in Direct Lake mode Power BI dashboards for near-real-time\ndecision-making.\n\n## Problem\n\nCommodity and market data arrives continuously from external feeds, but the\ndecisions it should inform are time-sensitive. A traditional import-and-refresh\nBI setup puts a scheduled refresh between the data and the decision — the\ndashboard is always some hours behind reality, and the gap is invisible to\nwhoever is reading it.\n\nThe platform's job is to get raw market feeds into a trustworthy, query-ready\nshape and in front of decision-makers with as little latency as the reporting\nlayer allows.\n\n## Architecture\n\nFive stages, implemented as a **Microsoft Fabric medallion architecture** with a\nDirect Lake reporting layer. These stage descriptions are migrated verbatim from\nthe interactive pipeline diagram on the previous site.\n\n**1. Raw Extraction** *(icon: Database)*\nRaw commodity and market data is pulled from source feeds into the pipeline,\nunchanged and untransformed — the entry point for everything downstream.\n\n**2. Bronze — Raw Ingest** *(icon: Layers)*\nExtracted data lands in the `bronze` layer exactly as received — a raw,\nappend-only landing zone that preserves the original records for lineage and\nreprocessing.\n\n**3. Silver — Cleansed** *(icon: Filter)*\nBronze records are cleaned, validated, and conformed into a consistent schema in\nthe `silver` layer, removing duplicates and standardizing formats.\n\n**4. Gold — Curated** *(icon: Gem)*\nSilver data is aggregated into business-ready tables in the `gold` layer — the\ncurated, query-optimized layer that reporting tools read from.\n\n**5. Direct Lake Dashboards** *(icon: LayoutDashboard, status: active)*\nPower BI queries the `gold` layer directly in `Direct Lake` mode — no import or\nscheduled refresh — so dashboards reflect near-real-time data for\ndecision-making.\n\n## Key decisions\n\n**Direct Lake instead of Import mode.** This is the decision that makes the\n\"near-real-time\" claim true rather than aspirational. Import mode would put a\nscheduled refresh between the gold layer and the dashboard; Direct Lake has\nPower BI read the gold tables in place, so there is no refresh window to fall\nbehind in.\n\n**Append-only bronze.** Landing data exactly as received — untransformed — costs\nstorage but buys lineage and full reprocessing. If a transformation rule turns\nout to be wrong, silver and gold can be rebuilt from bronze without going back\nto the source feeds.\n\n**Cleaning in silver, aggregation in gold.** Keeping deduplication and schema\nconformance separate from business aggregation means each layer has one job, and\na bad number can be traced to the layer that produced it.\n\n**Gold optimized for query, not for completeness.** The gold layer is shaped for\nwhat the dashboards ask, which is what allows Direct Lake to stay fast.\n\n## Challenges\n\n**Direct Lake has constraints Import mode doesn't.** Reading gold tables in place\nmeans the gold layer's physical shape directly determines dashboard performance\n— modeling mistakes surface as slow visuals rather than slow refreshes.\n\n**Schema drift from upstream feeds.** External commodity/market sources change\nshape without notice, and an append-only bronze layer means the drift lands\nintact and has to be absorbed in the bronze → silver transformation.\n\n**Deduplication across an append-only landing zone.** Because bronze never\noverwrites, silver carries the full burden of resolving duplicate and superseded\nrecords into a single consistent view.\n\n## Impact\n\nDelivered near-real-time commodity and market visibility to decision-makers by\nremoving the scheduled-refresh lag between the curated data layer and the\ndashboards reading it."
  },
  {
    "id": 3,
    "slug": "customer-segmentation-churn-prediction",
    "title": "AI-Enabled Customer Segmentation & Churn Prediction System",
    "desc": "Developed predictive models using EDA and behavioral analytics on 100K+ customer records to identify churn risk and enable next-best-action recommendations, improving targeted retention by 12%.",
    "tech": "Python, Pandas, NumPy, scikit-learn, Predictive Modeling, EDA, Classification, Clustering",
    "categories": [
      "AI",
      "Data"
    ],
    "skills": [
      "Predictive Modeling",
      "EDA",
      "Classification",
      "Clustering",
      "Behavioral Analytics"
    ],
    "stack": [
      "Python",
      "Pandas",
      "NumPy",
      "scikit-learn"
    ],
    "timeframe": "",
    "links": {
      "repo": "",
      "demo": "",
      "writeup": ""
    },
    "sections": {
      "problem": "Retention spend is usually distributed by intuition: everyone who \"looks at\nrisk\" gets the same offer, and the team finds out months later whether it\nworked. With 100K+ customer records of behavioral data available, the better\nquestion is which customers are actually likely to churn, which behavioral\npatterns separate them, and what the appropriate next action is for each\nsegment — rather than one blanket campaign.",
      "architecture": "A two-stage modeling pipeline over behavioral customer data:\n\n- **EDA layer** — exploratory analysis over 100K+ customer records in Pandas and\n  NumPy to establish the behavioral features that actually separate retained\n  from churned customers.\n- **Clustering / segmentation** — unsupervised grouping of customers into\n  behavioral segments, so downstream actions can be tailored per segment rather\n  than per individual.\n- **Churn classification** — supervised scikit-learn classifiers producing a\n  churn-risk score per customer.\n- **Next-best-action layer** — the segment and the risk score combine into a\n  recommended retention action, which is what drives the targeting.",
      "decisions": "**Segment first, then score.** Clustering before classification means the churn\nsignal is interpreted in context — a drop in activity means something different\nfor a high-frequency segment than for an occasional one.\n\n**Behavioral features over demographic ones.** The analysis leans on what\ncustomers *do* rather than who they are, which generalizes better and is more\nactionable for retention.\n\n**Ship a next-best-action, not a risk score.** A probability column doesn't\nchange anyone's behavior. Mapping score plus segment onto a concrete recommended\naction is what made the model usable for targeting.",
      "challenges": "**Class imbalance.** Churners are the minority class in most retention datasets,\nwhich makes naive accuracy misleading and forces careful choice of evaluation\nmetric and sampling strategy.\n\n**Defining churn.** \"Churned\" is a business definition, not a data one — the\nwindow and the threshold have to be chosen before any model can be trained, and\nthe choice materially changes the label distribution.\n\n**Making segments actionable.** Clusters that are statistically clean but don't\nmap to anything the retention team can actually do are a dead end; the\nsegmentation had to be legible as well as separable.",
      "impact": "Improved targeted retention by **12%**, driven by replacing untargeted retention\noutreach with segment-aware, risk-scored next-best-action recommendations across\na base of **100K+ customer records**."
    },
    "sourceFile": "knowledge/projects/customer-segmentation-churn-prediction.md",
    "bodyMarkdown": "Developed predictive models using EDA and behavioral analytics on 100K+ customer\nrecords to identify churn risk and enable next-best-action recommendations,\nimproving targeted retention by 12%.\n\n## Problem\n\nRetention spend is usually distributed by intuition: everyone who \"looks at\nrisk\" gets the same offer, and the team finds out months later whether it\nworked. With 100K+ customer records of behavioral data available, the better\nquestion is which customers are actually likely to churn, which behavioral\npatterns separate them, and what the appropriate next action is for each\nsegment — rather than one blanket campaign.\n\n## Architecture\n\nA two-stage modeling pipeline over behavioral customer data:\n\n- **EDA layer** — exploratory analysis over 100K+ customer records in Pandas and\n  NumPy to establish the behavioral features that actually separate retained\n  from churned customers.\n- **Clustering / segmentation** — unsupervised grouping of customers into\n  behavioral segments, so downstream actions can be tailored per segment rather\n  than per individual.\n- **Churn classification** — supervised scikit-learn classifiers producing a\n  churn-risk score per customer.\n- **Next-best-action layer** — the segment and the risk score combine into a\n  recommended retention action, which is what drives the targeting.\n\n## Key decisions\n\n**Segment first, then score.** Clustering before classification means the churn\nsignal is interpreted in context — a drop in activity means something different\nfor a high-frequency segment than for an occasional one.\n\n**Behavioral features over demographic ones.** The analysis leans on what\ncustomers *do* rather than who they are, which generalizes better and is more\nactionable for retention.\n\n**Ship a next-best-action, not a risk score.** A probability column doesn't\nchange anyone's behavior. Mapping score plus segment onto a concrete recommended\naction is what made the model usable for targeting.\n\n## Challenges\n\n**Class imbalance.** Churners are the minority class in most retention datasets,\nwhich makes naive accuracy misleading and forces careful choice of evaluation\nmetric and sampling strategy.\n\n**Defining churn.** \"Churned\" is a business definition, not a data one — the\nwindow and the threshold have to be chosen before any model can be trained, and\nthe choice materially changes the label distribution.\n\n**Making segments actionable.** Clusters that are statistically clean but don't\nmap to anything the retention team can actually do are a dead end; the\nsegmentation had to be legible as well as separable.\n\n## Impact\n\nImproved targeted retention by **12%**, driven by replacing untargeted retention\noutreach with segment-aware, risk-scored next-best-action recommendations across\na base of **100K+ customer records**."
  },
  {
    "id": 4,
    "slug": "parcelpal-route-optimization",
    "title": "ParcelPal – Route Optimization",
    "desc": "Built a delivery route optimizer with Dijkstra’s Algorithm, improving efficiency and reducing operational costs.",
    "tech": "JavaScript, React, Node.js, Express, MongoDB, REST APIs",
    "categories": [
      "Software",
      "Data"
    ],
    "skills": [
      "Algorithms",
      "Graph Search",
      "Full-Stack Development",
      "REST API Design"
    ],
    "stack": [
      "JavaScript",
      "React",
      "Node.js",
      "Express",
      "MongoDB",
      "REST APIs"
    ],
    "timeframe": "",
    "links": {
      "repo": "",
      "demo": "",
      "writeup": ""
    },
    "sections": {
      "problem": "Delivery routing done by hand — or by \"whatever order the addresses came in\" —\nburns time and fuel on paths nobody chose deliberately. For a small delivery\noperation, the cost shows up as longer routes, more driver hours, and higher\nper-parcel operating cost, all of which are avoidable with a proper shortest-path\ncomputation over the delivery network.\n\nParcelPal exists to compute those routes automatically and expose them through\nan interface a dispatcher can actually use.",
      "architecture": "A conventional **MERN-shaped full-stack application** with the optimization\nlogic on the server:\n\n- **React front end** — where routes are entered, computed, and displayed.\n- **Node.js + Express API** — the REST layer between the client and the routing\n  engine, exposing route-computation and data endpoints.\n- **Dijkstra's Algorithm** on the server — the delivery network is modeled as a\n  weighted graph, and shortest paths are computed across it to produce the\n  optimized route.\n- **MongoDB** — persistence for the delivery/route data the graph is built from.",
      "decisions": "**Dijkstra rather than a heuristic solver.** For a weighted, non-negative\ndelivery graph, Dijkstra gives a provably optimal shortest path without the\ntuning burden of a metaheuristic. The correctness guarantee was worth more than\nthe extra flexibility of an approximate optimizer at this scale.\n\n**Routing on the server, not the client.** Keeping the graph and the algorithm\nbehind the REST API means the client stays thin and the optimization logic can\nchange without shipping a new front end.\n\n**MongoDB for the data layer.** A document store fits delivery records that vary\nin shape better than a rigid relational schema for a project at this size.",
      "challenges": "**Modeling the delivery network as a graph.** The real work in a Dijkstra-based\noptimizer isn't the algorithm — it's deciding what the nodes and edge weights\nactually are (distance? time? cost?) so that \"shortest\" means the thing the\nbusiness cares about.\n\n**Keeping the graph and the stored data in sync.** Route data lives in MongoDB\nand has to be projected into a graph structure on each computation, which\nconstrains how large the network can get before the projection itself becomes\nthe bottleneck.",
      "impact": "Improved delivery efficiency and reduced operational costs by replacing manual\nroute selection with computed shortest paths."
    },
    "sourceFile": "knowledge/projects/parcelpal-route-optimization.md",
    "bodyMarkdown": "Built a delivery route optimizer with Dijkstra's Algorithm, improving efficiency\nand reducing operational costs.\n\n## Problem\n\nDelivery routing done by hand — or by \"whatever order the addresses came in\" —\nburns time and fuel on paths nobody chose deliberately. For a small delivery\noperation, the cost shows up as longer routes, more driver hours, and higher\nper-parcel operating cost, all of which are avoidable with a proper shortest-path\ncomputation over the delivery network.\n\nParcelPal exists to compute those routes automatically and expose them through\nan interface a dispatcher can actually use.\n\n## Architecture\n\nA conventional **MERN-shaped full-stack application** with the optimization\nlogic on the server:\n\n- **React front end** — where routes are entered, computed, and displayed.\n- **Node.js + Express API** — the REST layer between the client and the routing\n  engine, exposing route-computation and data endpoints.\n- **Dijkstra's Algorithm** on the server — the delivery network is modeled as a\n  weighted graph, and shortest paths are computed across it to produce the\n  optimized route.\n- **MongoDB** — persistence for the delivery/route data the graph is built from.\n\n## Key decisions\n\n**Dijkstra rather than a heuristic solver.** For a weighted, non-negative\ndelivery graph, Dijkstra gives a provably optimal shortest path without the\ntuning burden of a metaheuristic. The correctness guarantee was worth more than\nthe extra flexibility of an approximate optimizer at this scale.\n\n**Routing on the server, not the client.** Keeping the graph and the algorithm\nbehind the REST API means the client stays thin and the optimization logic can\nchange without shipping a new front end.\n\n**MongoDB for the data layer.** A document store fits delivery records that vary\nin shape better than a rigid relational schema for a project at this size.\n\n## Challenges\n\n**Modeling the delivery network as a graph.** The real work in a Dijkstra-based\noptimizer isn't the algorithm — it's deciding what the nodes and edge weights\nactually are (distance? time? cost?) so that \"shortest\" means the thing the\nbusiness cares about.\n\n**Keeping the graph and the stored data in sync.** Route data lives in MongoDB\nand has to be projected into a graph structure on each computation, which\nconstrains how large the network can get before the projection itself becomes\nthe bottleneck.\n\n## Impact\n\nImproved delivery efficiency and reduced operational costs by replacing manual\nroute selection with computed shortest paths."
  },
  {
    "id": 5,
    "slug": "sap-bw-data-integration",
    "title": "SAP BW data integration",
    "desc": "Integrates SAP BW as a source system into the same Fabric-based pipeline, handling extraction and transformation into the medallion layers feeding downstream reporting.",
    "tech": "SAP BW, Microsoft Fabric, Medallion Architecture (Bronze/Silver/Gold)",
    "categories": [
      "Data"
    ],
    "skills": [
      "Data Engineering",
      "Enterprise System Integration",
      "Medallion Architecture",
      "ETL"
    ],
    "stack": [
      "SAP BW",
      "Microsoft Fabric",
      "Medallion Architecture (Bronze/Silver/Gold)"
    ],
    "timeframe": "",
    "links": {
      "repo": "",
      "demo": "",
      "writeup": ""
    },
    "sections": {
      "problem": "Enterprise reporting data doesn't only come from external feeds — a large share\nof it lives in SAP BW, behind its own extraction model and semantics. \n\nThe work here was to bring SAP BW in as *source* on the\nmedallion pipeline.",
      "architecture": "Five stages sharing the same Fabric medallion pipeline used by the\n[Commodity Intelligence Platform](./commodity-intelligence-platform.md). These\nstage descriptions are migrated verbatim from the interactive pipeline diagram on\nthe previous site.\n\n**1. SAP BW Source** *(icon: Server)*\n`SAP BW` acts as the upstream enterprise source system, supplying the data this\npipeline extracts and feeds into the same Fabric-based medallion architecture\nused across the platform.\n\n**2. Bronze — Raw Ingest** *(icon: Layers)*\nData extracted from SAP BW lands in the `bronze` layer as raw, unmodified\nrecords — the same shared landing zone used by the platform's other source\nfeeds.\n\n**3. Silver — Transformed** *(icon: Filter)*\nRaw SAP BW extracts are transformed and conformed into a consistent schema in\nthe `silver` layer, aligning them with the rest of the medallion pipeline.\n\n**4. Gold — Curated** *(icon: Gem)*\nTransformed data is curated into business-ready tables in the `gold` layer, ready\nto feed downstream consumers.\n\n**5. Downstream Reporting** *(icon: BarChart3, status: active)*\nGold-layer data feeds downstream reporting — the same shared pipeline used by the\nplatform's other dashboards.",
      "decisions": "**One pipeline, many sources.** The defining decision: SAP BW is onboarded as a\nsource feed into the bronze layer . Transformation logic, curation rules, and the reporting layer are\nshared, so a business definition lives in exactly one place.\n\n**Conform to the shared schema in silver.** SAP BW's native structures are\nabsorbed and aligned at the silver stage, which is what lets everything\ndownstream stay source-agnostic.\n\n**Bronze stays raw for SAP BW too.** The same append-only, unmodified landing\nrule applies, preserving the original extracts for lineage and reprocessing.",
      "challenges": "**SAP BW's extraction model is its own discipline.** Getting data out of BW\nfaithfully — with its InfoProviders, hierarchies, and extractor semantics —\nis a different problem from reading a flat external feed, and the fidelity of\neverything downstream depends on it.\n\n**Conforming enterprise semantics ** SAP BW carries its own\nnaming and modeling conventions; reconciling them with the schema used by the\nplatform's other sources is the substance of the bronze → silver transformation.",
      "impact": "Brought an enterprise SAP BW source into the Fabric medallion pipeline\nwithout standing up a parallel stack, so downstream reporting consumes SAP BW\ndata through the same curated gold layer as every other source."
    },
    "pipelineStages": [
      {
        "id": "sap-bw",
        "label": "SAP BW Source",
        "description": "`SAP BW` acts as the upstream enterprise source system, supplying the data this pipeline extracts and feeds into the same Fabric-based medallion architecture used across the platform.",
        "icon": "Server",
        "status": "idle"
      },
      {
        "id": "bronze",
        "label": "Bronze — Raw Ingest",
        "description": "Data extracted from SAP BW lands in the `bronze` layer as raw, unmodified records — the same shared landing zone used by the platform's other source feeds.",
        "icon": "Layers",
        "status": "idle"
      },
      {
        "id": "silver",
        "label": "Silver — Transformed",
        "description": "Raw SAP BW extracts are transformed and conformed into a consistent schema in the `silver` layer, aligning them with the rest of the medallion pipeline.",
        "icon": "Filter",
        "status": "idle"
      },
      {
        "id": "gold",
        "label": "Gold — Curated",
        "description": "Transformed data is curated into business-ready tables in the `gold` layer, ready to feed downstream consumers.",
        "icon": "Gem",
        "status": "idle"
      },
      {
        "id": "reporting",
        "label": "Downstream Reporting",
        "description": "Gold-layer data feeds downstream reporting — the same shared pipeline used by the platform's other dashboards.",
        "icon": "BarChart3",
        "status": "active"
      }
    ],
    "pipelineLegend": [
      "SAP BW source",
      "shared medallion pipeline"
    ],
    "sourceFile": "knowledge/projects/sap-bw-data-integration.md",
    "bodyMarkdown": "Integrates SAP BW as a source system into the Fabric-based pipeline,\nhandling extraction and transformation into the medallion layers feeding\ndownstream reporting.\nImplemented cost analytics , of 50+ plants , analysing and giving cost breakdown of future and current cost of products according to raw materials\n\n## Problem\n\nEnterprise reporting data doesn't only come from external feeds — a large share\nof it lives in SAP BW, behind its own extraction model and semantics. \n\nThe work here was to bring SAP BW in as *source* on the\nmedallion pipeline.\n\n## Architecture\n\nFive stages sharing the same Fabric medallion pipeline used by the\n[Commodity Intelligence Platform](./commodity-intelligence-platform.md). These\nstage descriptions are migrated verbatim from the interactive pipeline diagram on\nthe previous site.\n\n**1. SAP BW Source** *(icon: Server)*\n`SAP BW` acts as the upstream enterprise source system, supplying the data this\npipeline extracts and feeds into the same Fabric-based medallion architecture\nused across the platform.\n\n**2. Bronze — Raw Ingest** *(icon: Layers)*\nData extracted from SAP BW lands in the `bronze` layer as raw, unmodified\nrecords — the same shared landing zone used by the platform's other source\nfeeds.\n\n**3. Silver — Transformed** *(icon: Filter)*\nRaw SAP BW extracts are transformed and conformed into a consistent schema in\nthe `silver` layer, aligning them with the rest of the medallion pipeline.\n\n**4. Gold — Curated** *(icon: Gem)*\nTransformed data is curated into business-ready tables in the `gold` layer, ready\nto feed downstream consumers.\n\n**5. Downstream Reporting** *(icon: BarChart3, status: active)*\nGold-layer data feeds downstream reporting — the same shared pipeline used by the\nplatform's other dashboards.\n\n## Key decisions\n\n**One pipeline, many sources.** The defining decision: SAP BW is onboarded as a\nsource feed into the bronze layer . Transformation logic, curation rules, and the reporting layer are\nshared, so a business definition lives in exactly one place.\n\n**Conform to the shared schema in silver.** SAP BW's native structures are\nabsorbed and aligned at the silver stage, which is what lets everything\ndownstream stay source-agnostic.\n\n**Bronze stays raw for SAP BW too.** The same append-only, unmodified landing\nrule applies, preserving the original extracts for lineage and reprocessing.\n\n## Challenges\n\n**SAP BW's extraction model is its own discipline.** Getting data out of BW\nfaithfully — with its InfoProviders, hierarchies, and extractor semantics —\nis a different problem from reading a flat external feed, and the fidelity of\neverything downstream depends on it.\n\n**Conforming enterprise semantics ** SAP BW carries its own\nnaming and modeling conventions; reconciling them with the schema used by the\nplatform's other sources is the substance of the bronze → silver transformation.\n\n## Impact\n\nBrought an enterprise SAP BW source into the Fabric medallion pipeline\nwithout standing up a parallel stack, so downstream reporting consumes SAP BW\ndata through the same curated gold layer as every other source."
  },
  {
    "id": 6,
    "slug": "supplysightai-agentic-supply-chain-intelligence",
    "title": "SupplySightAI – Agentic Supply Chain Intelligence",
    "desc": "Built an agentic AI platform that autonomously analyzes supply chain data, detects risks, explains root causes, forecasts disruptions, and recommends prioritized actions.",
    "tech": "Python, Pandas, Scikit-learn, Streamlit, Multi-Agent AI",
    "categories": [
      "AI",
      "Software"
    ],
    "skills": [
      "Multi-Agent AI",
      "Agentic Workflows",
      "Machine Learning",
      "Forecasting",
      "Root Cause Analysis",
      "Data Analytics"
    ],
    "stack": [
      "Python",
      "Pandas",
      "Scikit-learn",
      "Streamlit",
      "Multi-Agent AI"
    ],
    "timeframe": "",
    "links": {
      "repo": "",
      "demo": "",
      "writeup": ""
    },
    "sections": {
      "problem": "Supply chain teams sit on more operational data than they can read. The signal\nthat matters — a supplier slipping, a lane degrading, a stock position about to\ngo negative — is buried in tables that only get looked at after something has\nalready broken. The usual answer is a dashboard, but a dashboard only shows you\nwhat you already thought to ask for. It doesn't tell you *why* a number moved,\nwhat's likely to happen next, or which of the fifteen things now on fire you\nshould handle first.\n\nSupplySightAI was built to close that gap: instead of a human driving the\nanalysis, agents drive it and hand the human a ranked set of actions.",
      "architecture": "The system is organized as a **multi-agent pipeline**, where each agent owns one\nstage of the reasoning chain rather than one monolithic model trying to do\neverything:\n\n- **Analysis agent** — ingests supply chain data and computes the descriptive\n  layer using Pandas: current state, deltas, and anomalies against baseline.\n- **Risk detection agent** — flags where the data crosses into risk territory,\n  turning raw anomalies into named risks.\n- **Root cause agent** — explains *why* a flagged risk appeared, tracing it back\n  through the contributing dimensions rather than just reporting the symptom.\n- **Forecasting agent** — projects disruption forward using scikit-learn models,\n  so the output is anticipatory rather than purely retrospective.\n- **Recommendation agent** — ranks the resulting findings into a prioritized\n  action list, which is what the user actually sees first.\n\n**Streamlit** is the delivery layer: it hosts the interactive front end where a\nuser inspects the agents' findings and drills from the recommendation back down\nto the evidence.",
      "decisions": "**Agents over a single prompt.** Splitting analysis, risk, root cause,\nforecasting, and recommendation into separate agents means each stage has a\nnarrow, checkable job. It also means a failure is localizable — a bad\nrecommendation can be traced to the specific upstream agent that produced the\nfaulty input.\n\n**Classical ML for the numbers, agents for the reasoning.** Forecasting and\nanomaly detection run on scikit-learn and Pandas rather than being asked of a\nlanguage model. The agentic layer orchestrates and explains; it doesn't do\narithmetic it can't be held to.\n\n**Recommendations must be prioritized, not just produced.** An unranked list of\ntwelve risks is the same problem as the original dashboard. Ranking is treated\nas a first-class output, not a presentation detail.\n\n**Streamlit for the interface.** Chosen to keep the surface area small and the\niteration loop fast — the interesting work is the agent system, not the UI\nframework.",
      "challenges": "**Root cause is harder than detection.** Detecting that a metric moved is\nmechanical; attributing it correctly is not. The explanation layer has to\ndistinguish a genuine driver from a correlated bystander, and a confidently\nwrong root cause is worse than no root cause at all.\n\n**Keeping agents grounded in the data.** Every claim an agent makes has to trace\nback to a row it actually read. Without that constraint, an explanation layer\ndrifts into plausible-sounding narrative.\n\n**Ranking across incomparable risks.** Prioritization requires putting a\nforecast-driven risk and a currently-live anomaly on the same scale, which is a\njudgment call encoded in the system rather than something the data hands you.",
      "impact": "The platform turns supply chain monitoring from a reactive dashboard-reading\nexercise into an autonomous analysis loop that surfaces ranked, explained\nactions."
    },
    "sourceFile": "knowledge/projects/supplysightai-agentic-supply-chain-intelligence.md",
    "bodyMarkdown": "Built an agentic AI platform that autonomously analyzes supply chain data,\ndetects risks, explains root causes, forecasts disruptions, and recommends\nprioritized actions.\n\n## Problem\n\nSupply chain teams sit on more operational data than they can read. The signal\nthat matters — a supplier slipping, a lane degrading, a stock position about to\ngo negative — is buried in tables that only get looked at after something has\nalready broken. The usual answer is a dashboard, but a dashboard only shows you\nwhat you already thought to ask for. It doesn't tell you *why* a number moved,\nwhat's likely to happen next, or which of the fifteen things now on fire you\nshould handle first.\n\nSupplySightAI was built to close that gap: instead of a human driving the\nanalysis, agents drive it and hand the human a ranked set of actions.\n\n## Architecture\n\nThe system is organized as a **multi-agent pipeline**, where each agent owns one\nstage of the reasoning chain rather than one monolithic model trying to do\neverything:\n\n- **Analysis agent** — ingests supply chain data and computes the descriptive\n  layer using Pandas: current state, deltas, and anomalies against baseline.\n- **Risk detection agent** — flags where the data crosses into risk territory,\n  turning raw anomalies into named risks.\n- **Root cause agent** — explains *why* a flagged risk appeared, tracing it back\n  through the contributing dimensions rather than just reporting the symptom.\n- **Forecasting agent** — projects disruption forward using scikit-learn models,\n  so the output is anticipatory rather than purely retrospective.\n- **Recommendation agent** — ranks the resulting findings into a prioritized\n  action list, which is what the user actually sees first.\n\n**Streamlit** is the delivery layer: it hosts the interactive front end where a\nuser inspects the agents' findings and drills from the recommendation back down\nto the evidence.\n\n## Key decisions\n\n**Agents over a single prompt.** Splitting analysis, risk, root cause,\nforecasting, and recommendation into separate agents means each stage has a\nnarrow, checkable job. It also means a failure is localizable — a bad\nrecommendation can be traced to the specific upstream agent that produced the\nfaulty input.\n\n**Classical ML for the numbers, agents for the reasoning.** Forecasting and\nanomaly detection run on scikit-learn and Pandas rather than being asked of a\nlanguage model. The agentic layer orchestrates and explains; it doesn't do\narithmetic it can't be held to.\n\n**Recommendations must be prioritized, not just produced.** An unranked list of\ntwelve risks is the same problem as the original dashboard. Ranking is treated\nas a first-class output, not a presentation detail.\n\n**Streamlit for the interface.** Chosen to keep the surface area small and the\niteration loop fast — the interesting work is the agent system, not the UI\nframework.\n\n## Challenges\n\n**Root cause is harder than detection.** Detecting that a metric moved is\nmechanical; attributing it correctly is not. The explanation layer has to\ndistinguish a genuine driver from a correlated bystander, and a confidently\nwrong root cause is worse than no root cause at all.\n\n**Keeping agents grounded in the data.** Every claim an agent makes has to trace\nback to a row it actually read. Without that constraint, an explanation layer\ndrifts into plausible-sounding narrative.\n\n**Ranking across incomparable risks.** Prioritization requires putting a\nforecast-driven risk and a currently-live anomaly on the same scale, which is a\njudgment call encoded in the system rather than something the data hands you.\n\n## Impact\n\nThe platform turns supply chain monitoring from a reactive dashboard-reading\nexercise into an autonomous analysis loop that surfaces ranked, explained\nactions."
  }
];

/**
 * Documented achievements. Currently EMPTY: knowledge/achievements.md still
 * holds 2 unfilled placeholder section(s), which are excluded
 * rather than shipped. Consumers must handle the empty case.
 */
export const achievements: Achievement[] = [
  {
    "id": "winner-at-nrf-x-arcade-marketing-strategy-hackathon",
    "title": "Winner at NRF X Arcade Marketing Strategy Hackathon",
    "description": "Designed a strategic solution driving measurable business outcomes, presenting to technical and \nbusiness stakeholders.",
    "date": "Apr 2026",
    "linkedinUrl": "https://www.linkedin.com/posts/activity-7454258413708107776-3Xwj?utm_source=share&utm_medium=member_desktop&rcm=ACoAADQ56VsBb9KhaedG8TtwCN3lrXIjmyFVIL4",
    "tags": [
      "1st Place"
    ]
  },
  {
    "id": "celonis-agent-a-thon",
    "title": "Celonis Agent-a-thon",
    "description": "Devised an autonomous agentic solution with evaluation frameworks and guardrail mechanisms under \ncompetitive conditions",
    "date": "Nov 2025",
    "linkedinUrl": "(https://www.linkedin.com/posts/activity-7389159466169970688-5Z3F?utm_source=share&utm_medium=member_desktop&rcm=ACoAADQ56VsBb9KhaedG8TtwCN3lrXIjmyFVIL4)",
    "tags": [
      "3rd Place"
    ]
  }
];

export interface SystemSection {
  heading: string;
  markdown: string;
}

export interface ADR {
  id: string;
  title: string;
  context: string;
  decision: string;
  tradeoffs: string;
  consequences: string;
  wordCount: number;
}

/** Narrative sections of knowledge/system-architecture.md, excluding ADRs. */
export const systemOverview: SystemSection[] = [
  {
    "heading": "Overview",
    "markdown": "This site is not a portfolio with a chatbot bolted on. The same content powers\nboth halves: a directory of markdown files under `/knowledge` is the single\nsource of truth, and everything else is derived from it at build time. The\nstatic pages render from it. The agent retrieves from it. There is no second\ncopy to drift.\n\nTwo build artifacts come out of `npm run knowledge:build`:\n\n- `src/data/site-content.ts` — typed data for the React pages, because a\n  browser cannot read markdown off disk.\n- `lib/knowledge/index.json` — 55 chunks with 384-dimension embeddings, used\n  for semantic retrieval at request time.\n\nBoth are generated. Neither is edited by hand. Editing a fact means editing the\nmarkdown and re-running the build, which is the only way the two can stay in\nagreement."
  },
  {
    "heading": "The orchestration graph",
    "markdown": "Every answer runs through a five-node LangGraph state machine:\n\n`classify → retrieve → [clarify] → act → generate`\n\n**classify** routes the question into one of seven intents\n(`project_deep_dive`, `experience`, `achievements`, `skills_query`,\n`architecture_meta`, `general_greeting`, `out_of_scope`) and tries to resolve a\nproject slug from it. Runs on Groq's `llama-3.1-8b-instant` at temperature 0 —\nrouting must be deterministic, and it sits on the hot path of every turn.\n\n**retrieve** embeds the question locally and runs a cosine-similarity scan over\n`index.json`, filtered by the intent's source type and, when known, the\nspecific project. No LLM call. Sub-millisecond.\n\n**clarify** is conditional. When router confidence is low, or the question is a\nproject deep-dive with no identifiable project, the graph calls LangGraph's\n`interrupt()` — suspending mid-execution and returning a question to the user\ninstead of guessing. The reply resumes the same run from the same checkpoint.\n\n**act** binds the tool registry to Gemini and lets it decide which structured\nfacts it needs. It never writes prose.\n\n**generate** turns the retrieved chunks and tool results into the answer, under\na system prompt loaded from `profile.md` at runtime."
  },
  {
    "heading": "Model choices",
    "markdown": "| Node | Model | Why |\n|---|---|---|\n| classify | Groq `llama-3.1-8b-instant` | Latency. Runs every turn, emits a label. |\n| act, generate | Google `gemini-2.0-flash` | Tool calling, long context, generous free tier. |\n| embeddings | `Xenova/all-MiniLM-L6-v2`, local | No API key, no per-query cost, 384 dimensions. |"
  },
  {
    "heading": "Grounding",
    "markdown": "The generation model is structurally prevented from answering off its own\nmemory. It receives only the retrieved chunks and the tool results, under\ninstructions authored in `profile.md` — not hard-coded in TypeScript — that\nrequire citing the source document and saying \"that's not something I've\ndocumented here\" rather than guessing. Tools strip `` review\ncomments and unfilled placeholder sections before anything reaches the model.\n\n---"
  }
];

/** Architecture Decision Records, parsed from the same file the agent
 *  retrieves from — the /system page and the agent cannot disagree. */
export const adrs: ADR[] = [
  {
    "id": "ADR-001",
    "title": "LangGraph over a single LangChain chain",
    "context": "The agent has to do five distinguishable things per question: work out what is\nbeing asked, fetch relevant context, decide whether it has enough to proceed,\ngather structured facts, and write an answer. The obvious first implementation\nis a single LangChain chain — one prompt, one model, tools bound, let it figure\nout the sequence.",
    "decision": "Use LangGraph and make each step an explicit node with typed state between\nthem.",
    "tradeoffs": "A single chain is less code and fewer moving parts. It also collapses five\ndecisions into one opaque one: when an answer is wrong, there is no way to tell\nwhether the model misunderstood the question, retrieved the wrong documents, or\nhad the right context and wrote badly. Debugging becomes prompt archaeology.\n\nThe stronger argument against LangGraph was the state machine's ceremony —\nannotations, reducers, conditional edges — for what starts as a linear\npipeline. That ceremony bought two things that turned out to matter. First,\nrouting: the intent classifier narrows retrieval to one slice of the knowledge\nbase, which measurably improves precision because a profile chunk can no longer\noutrank a project answer. Second, `interrupt()`, which is only expressible\nbecause execution is a resumable graph with checkpointed state. A chain cannot\npause halfway, ask the user something, and resume where it left off.",
    "consequences": "Every node is independently inspectable, and the site exposes that: the trace\npanel lights each node as it fires, so a visitor watches the machine work. A\nbad answer is attributable to a specific node. Adding a step means adding a\nnode and an edge, not rewriting a prompt.\n\nThe cost is real. There is more scaffolding than a chain needs, the state\nannotations are boilerplate, and a second retrieve node exists purely to\nguarantee termination after a clarify. For a five-node graph this is close to\nbreak-even; it pays off as soon as a sixth node is added.",
    "wordCount": 321
  },
  {
    "id": "ADR-002",
    "title": "A two-model cascade instead of one model everywhere",
    "context": "Every turn needs a routing decision and an answer. The simplest configuration\nis one capable model doing both.",
    "decision": "Route on Groq's `llama-3.1-8b-instant` at temperature 0; generate on Google's\n`gemini-2.0-flash` at temperature 0.3.",
    "tradeoffs": "One model is simpler: one key, one SDK, one failure mode, one bill. The case\nagainst it is that classification and generation want opposite things.\n\nRouting wants determinism and speed. It emits a label and maybe a slug, it runs\nbefore anything else, and its latency is added to every single turn. Asking a\nlarge multimodal model to do that is paying for capability the task never uses\nand adding delay the user always feels. Groq's inference speed is the point of\nusing it — the router should be effectively free.\n\nGeneration wants the opposite: a long context window to hold retrieved chunks\nand tool output, reliable tool calling, and enough fluency to write prose worth\nreading. Gemini Flash provides that with a free tier generous enough to develop\nagainst.\n\nThe real cost of the split is two provider dependencies, two ways to fail, and\ntwo rate limits. It also introduces a class of bug that does not exist with one\nmodel — the router returning an intent or slug the rest of the graph does not\nrecognize — which is why the router's output is validated against the real\nintent list and fuzzy-matched against the real project slugs rather than\ntrusted.",
    "consequences": "Both providers are reached through LangChain's model wrappers, so the graph\nonly ever sees `.invoke()` / `.stream()` / `.bindTools()`. Swapping either\nprovider is an edit to `lib/agent/models.ts` and nothing else. If Groq is\nunavailable, classification fails closed to low confidence, which routes to\nclarify — the agent asks a question rather than answering wrongly.",
    "wordCount": 297
  },
  {
    "id": "ADR-003",
    "title": "Local embeddings and a flat JSON index, not a vector database",
    "context": "Semantic retrieval needs embeddings and something to search them. The default\nanswer in 2026 is a hosted embedding API plus a managed vector database.",
    "decision": "Embed locally with `Xenova/all-MiniLM-L6-v2` via transformers.js. Store all 55\nchunks and their vectors in a single committed `index.json` and scan it\nlinearly in memory.",
    "tradeoffs": "At this scale the numbers make the decision. Fifty-five chunks at 384\ndimensions is a 237 KB file. A full cosine scan takes under a millisecond —\nfar less than the network round trip to any vector database, let alone the\nembedding API call that would precede it. A vector database would add a\nservice to run, a client to configure, an index to keep in sync, a bill, and\nanother thing that can be down at request time, in exchange for solving a\nproblem that does not exist yet.\n\nLocal embedding removes the other API call. Queries cost nothing, work offline,\nand need no key, which means a contributor can clone this repository and have\nretrieval working without signing up for anything.\n\nThe honest costs: the model is a ~23 MB download on first run and adds a\none-time load to a cold start. `@xenova/transformers` is no longer maintained\nand pulls transitive dependencies with known advisories. And a linear scan is\nO(n) — fine at 55 chunks, fine at 5,000, wrong at 5,000,000.",
    "consequences": "The index is a build artifact, committed and regenerated by\n`npm run knowledge:build`, never computed per request. `search.ts` refuses to\nload an index whose recorded model or dimension count does not match the\nruntime embedder — the failure mode it guards against is the quiet one, where\nmismatched vectors still produce plausible-looking similarity scores and\nsilently meaningless results.\n\nThe migration path, if the knowledge base grows two orders of magnitude, is to\nreplace one file. Nothing above `searchKnowledge()` knows how retrieval works.",
    "wordCount": 313
  },
  {
    "id": "ADR-004",
    "title": "Markdown files, not a database or CMS",
    "context": "Project write-ups, roles, and profile information need to live somewhere that\nboth the static pages and the agent can read.",
    "decision": "A `/knowledge` directory of markdown files with YAML frontmatter, versioned in\ngit alongside the code.",
    "tradeoffs": "A CMS gives a web editor, media handling, and scheduled publishing. A database\ngives queries and relations. Both were rejected for the same reason: they put\nthe content behind a service, and the content here is the product. Every fact\nthe agent can state has to be reviewable, diffable, and revertible. A bad claim\nabout a former employer should be catchable in a pull request, not discovered\nin production.\n\nMarkdown also makes the dual-consumption problem tractable. The same file\nsupplies frontmatter for the project card and prose for the agent to retrieve.\nChunking on `##` boundaries works because the documents are authored around\nthose boundaries — every project has Problem / Architecture / Key decisions /\nChallenges / Impact, so a chunk is almost always one complete thought.\n\nThe costs are real. There is no editing UI; changing content means editing a\nfile and running a build. Validation has to be built rather than enforced by a\nschema — `loader.ts` uses Zod for that and fails loudly with the file and field\nnamed. And there is no referential integrity, so a project's declared slug is\nchecked against its filename in code because nothing else would catch the\nmismatch.",
    "consequences": "Content review is code review. The migration that created this knowledge base\nproduced a `KNOWLEDGE-AUDIT.md` listing every drafted claim and every\n`` flag — 32 of them — so unconfirmed narrative is visible\nrather than quietly published. Those flags are stripped before anything reaches\nthe model or the page.",
    "wordCount": 291
  },
  {
    "id": "ADR-005",
    "title": "Interrupt and ask, instead of guessing",
    "context": "Some questions cannot be answered as asked. \"Tell me about the pipeline\nproject\" matches two projects. A vague question matches nothing well. A\nretrieval system will always return its best match, and a language model will\nalways write something confident about it.",
    "decision": "When router confidence falls below 0.6, or a project deep-dive resolves no\nproject, call LangGraph's `interrupt()` and ask the user which they meant.",
    "tradeoffs": "Guessing is smoother. The user asks once and gets an answer, and most of the\ntime the top match is right. The problem is the rest of the time: a confidently\nwrong answer about the wrong project is worse than a question, because the\nvisitor has no way to know it happened. On a site whose entire claim is that\nthe agent only says documented things, quietly answering the wrong question\nundermines the premise.\n\nThe alternative to interrupting was answering with a hedge — \"you might mean X\nor Y, here's X\". That is strictly worse: it still commits to a guess, and it\nburies the ambiguity in prose the user has to read carefully to notice.\n\nThe implementation cost is that the graph must be resumable, which requires a\ncheckpointer and a stable session id, and the API has to model a paused run as\na distinct state the client can respond to.",
    "consequences": "The clarifying question offers the closest two or three real projects as\nclickable chips, matched against actual slugs, so the user can answer in one\nclick. Selecting one resumes the suspended run from its checkpoint rather than\nstarting over.\n\nThe current checkpointer is `MemorySaver`, which is in-process. On a serverless\ndeployment a cold start between the question and the reply loses the paused\nrun. That is a known limitation, not a design choice — it wants a durable\ncheckpointer before this handles real traffic.",
    "wordCount": 312
  },
  {
    "id": "ADR-006",
    "title": "A generated content module, not a hand-maintained one",
    "context": "The static pages need the knowledge base's content, but the browser cannot read\nmarkdown from disk, and the loader depends on `node:fs`.",
    "decision": "Generate `src/data/site-content.ts` from `/knowledge` at build time. Never edit\nit by hand.",
    "tradeoffs": "The alternative was to keep maintaining a hand-written data file for the pages\nand let the agent read markdown separately. That is exactly the two-sources\nproblem this architecture exists to remove: the card would say one thing and\nthe agent another, and nothing would detect the divergence.\n\nParsing markdown in the browser was considered — Vite can inline files as raw\nstrings — but frontmatter parsing and validation belong at build time, where a\nmalformed file fails the build instead of the page.",
    "consequences": "`npm run knowledge:build` produces both artifacts in one step, so they cannot\nbe regenerated independently and fall out of sync. The generated file carries a\ndo-not-edit banner.\n\nThis decision was validated the hard way. When the pages were migrated onto the\ngenerated module, a 42-point fidelity check against the original hand-written\ndata file caught three real defects: soft-wrapped markdown bullets being\ntruncated at the wrap point, CRLF line endings silently breaking every heading\nregex, and four project one-liners that had been reworded during migration. All\nthree were invisible to the type checker and to a successful build.",
    "wordCount": 223
  }
];
